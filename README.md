# core-topo-gen
This application is enables the automatic and pseudo-automatic creation of cybersecurity capture-the-flag scenarios that use the common open research emulator as a for networks and hosts.

Steps to run
1. Install requirements

```
pip install -r requirements.txt
```
2. Start the GUI
```
python main.py
```