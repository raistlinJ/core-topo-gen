ctf:
  id: example-ctf
  version: "1.0"
  difficulty: medium
  description: Base schema for a DAG-based CTF

artifacts:
  types:
    Knowledge(ip): {}
    File(path): {}
    Credential(user, password): {}

challenges: []